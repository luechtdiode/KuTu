codecov:
  require_ci_to_pass: yes

coverage:
  round: up
  range: 18..100
  precision: 5
  
parsers:
  gcov:
    branch_detection:
      conditional: yes
      loop: yes
      method: yes
      macro: no

comment:
  layout: "reach,diff,flags,files,footer"
  behavior: default
  require_changes: no
